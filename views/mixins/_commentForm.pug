mixin commentForm(idea)
    form(action=`/comments/${idea._id}` method="POST")
        textarea(name="text" class="create_comment" placeholder="What do you think about this idea?..")
        input(type="submit" class="submit" value="Submit")